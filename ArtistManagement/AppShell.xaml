<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ArtistManagement.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ArtistManagement"
    xmlns:views="clr-namespace:ArtistManagement.Views"
    Shell.FlyoutBehavior="Flyout"
    Title="Artist Management">

    <FlyoutItem Title="Artists">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="Statistics">
        <ShellContent
            ContentTemplate="{DataTemplate views:ArtistStatisticsPage}"
            Route="Statistics" />
    </FlyoutItem>

</Shell>
